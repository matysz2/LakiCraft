apiVersion: apps/v1
kind: Deployment
metadata:
  name: lakicraft-postgres-deployment
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: lakicraft-postgres
  template:
    metadata:
      labels:
        app: lakicraft-postgres
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: lakicraft_prod
            - name: POSTGRES_USER
              value: lakicraft_user
            # Pobieranie hasła startowego bazy z bezpiecznego Secretu
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: lakicraft-secrets
                  key: db-password
---
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: default
spec:
  selector:
    app: lakicraft-postgres
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
  type: ClusterIP